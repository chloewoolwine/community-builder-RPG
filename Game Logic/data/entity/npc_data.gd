extends EntityData
class_name NpcData

var relationship : int
#this is probably going to become as complex as the inventorydata
var spoken_conversations: Array[int]
var unspoken_conversations: Array[int]

var story_flags: Array[int]
var stat_array: Array[int]
